{
  "BuggyMemoryLeak": {
    "thought": "<thought> The useEffect adds an event listener on mount, but does not return a cleanup function to remove it on unmount. This will cause a memory leak and duplicate listeners. </thought>",
    "diff": "<diff>\n@@ -4,4 +4,5 @@\n     window.addEventListener('resize', () => console.log('resize'));\n+    return () => window.removeEventListener('resize', () => console.log('resize'));\n   }, []);\n</diff>"
  },
  "BuggyStaleClosure": {
    "thought": "<thought> The setInterval captures the initial 'count' value of 0. Because the dependency array is empty, the closure becomes stale and it will repeatedly set the count to 1. The functional state updater form should be used to avoid the stale closure. </thought>",
    "diff": "<diff>\n@@ -6,3 +6,3 @@\n     const id = setInterval(() => {\n-      setCount(count + 1);\n+      setCount(c => c + 1);\n     }, 1000);\n</diff>"
  },
  "ArchitectureMonolith": {
    "thought": "<thought> This component does too much: fetching data, fetching user info, rendering main UI, and managing modal state. The modal state and user fetching should be abstracted into custom hooks or child components to avoid unnecessary re-renders of the entire giant component. </thought>",
    "diff": "<diff>\n@@ -3,2 +3,3 @@\n // Refactored monolith to separate components\n+import { useUser } from '../hooks/useUser';\n</diff>"
  },
  "PropDrilling": {
    "thought": "<thought> The Parent component takes many props (user, theme, onUpdate, settings, features) only to pass them directly down to Child. This is classic prop drilling. We should either use Context, component composition (passing children), or pass a configuration object. </thought>",
    "diff": "<diff>\n@@ -4,4 +4,4 @@\n-export const Parent = ({ user, theme, onUpdate, settings, features }) => {\n+export const Parent = ({ children }) => {\n   return (\n-      <Child user={user} theme={theme} onUpdate={onUpdate} settings={settings} features={features} />\n+      {children}\n   );\n</diff>"
  },
  "CleanHook": {
    "thought": "<thought> This is a clean, well-abstracted custom hook. It properly uses useCallback for the toggle function to prevent unnecessary reference changes. No architectural changes needed. </thought>",
    "diff": "<diff>\n// No changes required\n</diff>"
  },
  "ComplexComponent": {
    "thought": "<thought> The component properly abstracts logic to useToggle and memoizes the sorted items array to prevent expensive sorting on every render. However, the items array is passed as a prop, if it is a new array every time from the parent, the useMemo will run anyway. The architecture itself is sound. </thought>",
    "diff": "<diff>\n// No changes required\n</diff>"
  },
  "MissingDeps": {
    "thought": "<thought> The useEffect relies on fetchUser, which itself relies on the userId prop. Because the dependency array is empty, it will not re-fetch when userId changes, leading to stale data. userId and fetchUser should be in the dependency array, and fetchUser should be wrapped in useCallback. </thought>",
    "diff": "<diff>\n@@ -5,3 +5,3 @@\n-  const fetchUser = () => {\n+  const fetchUser = useCallback(() => {\n     fetch('/api/users/' + userId).then(res => res.json()).then(setUser);\n-  };\n+  }, [userId]);\n   useEffect(() => {\n     fetchUser();\n-  }, []);\n+  }, [fetchUser]);\n</diff>"
  },
  "CleanComponent": {
    "thought": "<thought> This component successfully abstracts the authentication logic to a custom hook and acts as a clean gatekeeper component. Excellent modularity. </thought>",
    "diff": "<diff>\n// No changes required\n</diff>"
  },
  "UselessMemo": {
    "thought": "<thought> The useMemo hook is being used to cache simple string concatenation. String concatenation is incredibly cheap in JS, making the overhead of useMemo (memory allocation and dependency checking) more expensive than the calculation itself. It should be removed. </thought>",
    "diff": "<diff>\n@@ -4,4 +4,2 @@\n-  const greeting = useMemo(() => {\n-    return \"Hello \" + name;\n-  }, [name]);\n+  const greeting = \"Hello \" + name;\n</diff>"
  },
  "GoodContext": {
    "thought": "<thought> The Context setup is good, separating the Provider and a custom useTheme hook for easier consumption. This is standard and correct React architecture. </thought>",
    "diff": "<diff>\n// No changes required\n</diff>"
  }
}
