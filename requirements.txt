# ============================================================
# Architect-JS — Root Python Requirements
# ============================================================
# Install with: pip install -r requirements.txt
# (Or run setup.bat / setup.sh)

# ── CLI & Terminal UI ────────────────────────────────────────
rich>=13.7.0
click>=8.1.7
python-dotenv>=1.0.0

# ── RAG Pipeline ─────────────────────────────────────────────
chromadb>=0.5.0
sentence-transformers>=3.0.0
pypdf>=4.2.0

# ── Data Mining ──────────────────────────────────────────────
pydriller>=2.6.0
GitPython>=3.1.41

# ── ML Training (optional, GPU/Colab only) ───────────────────
# Uncomment on a CUDA-enabled machine or in Google Colab:
# transformers>=4.40.0
# trl>=0.8.6
# datasets>=2.19.0
# torch>=2.2.0
# unsloth>=2024.5.1
